{
  "name": "sungsan-gnuboard-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/**/*.test.mjs",
    "build:css": "node tools/build-css.mjs",
    "export:setup-sql": "node tools/setup/export-setup-sql.mjs",
    "verify": "npm run test && npm run build:css"
  }
}
